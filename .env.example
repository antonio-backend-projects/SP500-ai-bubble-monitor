# Copia questo file in `.env` (nella root del repo) e inserisci le tue key.
# `.env` è in .gitignore — non va su git.

# FRED API — gratis: https://fredaccount.stlouisfed.org/apikeys
FRED_API_KEY=

# Opzionale: porta dashboard web
# WEB_PORT=8891
