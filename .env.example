# Copy this file to `.env` in the repo root and fill in your keys.
# `.env` is gitignored — do not commit it.

# FRED API — free: https://fredaccount.stlouisfed.org/apikeys
FRED_API_KEY=

# Optional: dashboard port (default 8891)
# WEB_PORT=8891
